# Local development only. Generate actual local credentials using npm run env:init.
POSTGRES_USER=family_owner
POSTGRES_PASSWORD=replace-with-local-random-password
POSTGRES_DB=family_dev
POSTGRES_PORT=54329
DATABASE_URL=postgresql://family_owner:replace-with-local-random-password@127.0.0.1:54329/family_dev
# No auth/LLM/provider credentials are needed to run the scaffold.
