import { PreviewShell } from '../preview-shell';
import { JoinFlow } from './join-flow';

export default function DesignPreviewJoin() {
  return (
    <PreviewShell>
      <main id="main">
        <JoinFlow />
      </main>
    </PreviewShell>
  );
}
